services:
  farnell-mcp:
    build:
      context: .
      target: production
    image: farnell-mcp:latest
    container_name: farnell-mcp-server
    stdin_open: true
    tty: true      
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
